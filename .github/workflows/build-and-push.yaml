name: Build and Push Docker Image

on:
  push:
    branches:
      - main  # Change this to your default branch if it's different

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}  # Docker Hub username stored in Secrets
          password: ${{ secrets.DOCKER_PASSWORD }}  # Docker Hub password stored in Secrets

      - name: Build Docker Image
        run: |
          docker build -t your-username/prefect-flow:1.0.0 .
          docker tag your-username/prefect-flow:1.0.0 your-username/prefect-flow:latest

      - name: Push Docker Image
        run: |
          docker push your-username/prefect-flow:1.0.0
          docker push your-username/prefect-flow:latest